# Frontend environment variables (Create React App)
# API domain used by frontend requests
REACT_APP_API_BASE_URL=http://localhost:3001

# Upload endpoint (can be full URL or API path)
REACT_APP_UPLOAD_ENDPOINT=/uploads/image
